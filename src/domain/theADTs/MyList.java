package domain.theADTs;


// The interface of a list which will hold strings
public interface MyList <T>{
    //The function add an element to the list, the parameter is a string format
    void addList(T s);
}
